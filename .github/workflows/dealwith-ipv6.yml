name: Process IPv6 Addresses

on:
  schedule:
    - cron: '0 */6 * * *'  # 每6小时运行一次
  workflow_dispatch:
  push:
    branches: [ main, master ]
    paths:
      - '.github/workflows/process-ipv6.yml'

permissions:
  contents: write

jobs:
  process-ipv6:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Download and process IPv6
      run: |
        set -e
        
        # 配置
        SOURCE_URL="https://raw.githubusercontent.com/ymyuuu/IPDB/refs/heads/main/BestCF/bestcfv6.txt"
        ORIGINAL_FILE="bestcfv6_original.txt"
        PROCESSED_FILE="ipv6_dealwith_ips.txt"
        
        echo "🔗 下载源: $SOURCE_URL"
        
        # 下载原始文件
        curl -s -f "$SOURCE_URL" -o "$ORIGINAL_FILE" || {
          echo "❌ 下载失败"
          exit 1
        }
        
        # 验证文件内容
        if [ ! -s "$ORIGINAL_FILE" ]; then
          echo "❌ 下载的文件为空"
          exit 1
        fi
        
        echo "📊 原始文件信息:"
        echo "  行数: $(wc -l < "$ORIGINAL_FILE")"
        echo "  大小: $(wc -c < "$ORIGINAL_FILE") 字节"
        
        # 创建处理脚本
        cat << 'SCRIPT_EOF' > process_ipv6.sh
#!/bin/bash

input_file="$1"
output_file="$2"

# 更严格的 IPv6 正则表达式
# 匹配以下格式:
# 1. 完整的 IPv6 (8组4位十六进制)
# 2. 压缩格式 (包含 ::)
# 3. 省略前导零
ipv6_regex='^(([0-9a-fA-F]{1,4}:){7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))$'

# 处理文件
processed_count=0
> "$output_file"  # 清空输出文件

while IFS= read -r line || [ -n "$line" ]; do
    # 去除前后空白字符
    line_clean=$(echo "$line" | xargs)
    
    # 跳过空行和注释行
    if [ -z "$line_clean" ] || [[ "$line_clean" == \#* ]]; then
        continue
    fi
    
    # 检查是否为 IPv6 地址
    if [[ "$line_clean" =~ $ipv6_regex ]]; then
        echo "[$line_clean]:443" >> "$output_file"
        ((processed_count++))
    else
        # 如果不是有效的 IPv6，可以选择记录到日志
        echo "⚠️  跳过无效行: $line_clean" >&2
    fi
done < "$input_file"

echo "✅ 处理完成: 输出了 $processed_count 个 IPv6 地址"
SCRIPT_EOF
        
        chmod +x process_ipv6.sh
        
        # 执行处理
        echo "🔄 正在处理 IPv6 地址..."
        ./process_ipv6.sh "$ORIGINAL_FILE" "$PROCESSED_FILE"
        
        # 显示结果
        echo "📋 结果预览:"
        echo "--- 前5行 ---"
        head -5 "$PROCESSED_FILE"
        echo "--- 后5行 ---"
        tail -5 "$PROCESSED_FILE"
        
        echo "📊 处理完成统计:"
        echo "  原始行数: $(wc -l < "$ORIGINAL_FILE")"
        echo "  处理后行数: $(wc -l < "$PROCESSED_FILE")"

    - name: Generate README
      run: |
        # 生成 README 文件
        cat > README_IPV6.md << 'EOF'
# IPv6 Address Processing Results

## 更新信息
- **最后更新时间**: $(date -u '+%Y-%m-%d %H:%M:%S UTC')
- **原始文件**: [bestcfv6_original.txt](bestcfv6_original.txt)
- **处理后文件**: [ipv6_dealwith_ips.txt](ipv6_dealwith_ips.txt)

## 统计信息
- 原始 IPv6 地址数: $(wc -l < bestcfv6_original.txt)
- 处理后的地址数: $(wc -l < ipv6_dealwith_ips.txt)

## 处理说明
将普通的 IPv6 地址转换为 `[IPv6]:443` 格式，用于代理或防火墙规则配置。

## 文件格式示例
### 原始格式
